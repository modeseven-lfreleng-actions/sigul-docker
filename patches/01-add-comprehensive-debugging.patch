--- a/src/utils.py
+++ b/src/utils.py
@@ -449,6 +449,10 @@ def nss_init(config):
     Raise NSSInitError.
 
     '''
+    logging.info('==================== NSS INITIALIZATION DEBUG ====================')
+    logging.info('NSS_DIR: %s', config.nss_dir)
+    logging.info('NSS_PASSWORD length: %d', len(config.nss_password) if config.nss_password else 0)
+    
     def _password_callback(unused_slot, retry):
         if not retry:
             return config.nss_password
@@ -456,9 +460,24 @@ def nss_init(config):
 
     nss.nss.set_password_callback(_password_callback)
     try:
+        logging.info('Calling nss.nss.nss_init(%s)', config.nss_dir)
         nss.nss.nss_init(config.nss_dir)
+        logging.info('✓ NSS database initialized successfully')
+        
         # Test the password
+        logging.info('Testing NSS password by authenticating key slot...')
         nss.nss.get_internal_key_slot().authenticate()
+        logging.info('✓ NSS password authentication successful')
+        
+        # List available certificates for debugging
+        try:
+            import nss.nss
+            cert_db = nss.nss.get_default_certdb()
+            certs = cert_db.find_certs_from_nickname('*', True)
+            logging.info('Available certificates in NSS database:')
+            for cert in certs:
+                logging.info('  - %s', cert.subject)
+        except Exception as e:
+            logging.debug('Could not list certificates: %s', e)
     except nss.error.NSPRError as e:
         if e.errno == nss.error.SEC_ERROR_BAD_DATABASE:
             raise NSSInitError(
@@ -466,6 +485,7 @@ def nss_init(config):
                     config.nss_dir))
         elif e.errno == nss.error.SEC_ERROR_BAD_PASSWORD:
             raise NSSInitError('Provided NSS password is incorrect')
+        logging.error('NSS initialization failed with NSPR error %d: %s', e.errno, str(e))
         raise
     nss.ssl.set_domestic_policy()
     nss.ssl.set_ssl_default_option(nss.ssl.SSL_ENABLE_SSL2, False)
@@ -475,6 +495,7 @@ def nss_init(config):
     min_tls = nss.ssl.ssl_library_version_from_name(config.nss_min_tls)
     max_tls = nss.ssl.ssl_library_version_from_name(config.nss_max_tls)
     nss.ssl.ssl_version_range_set(nss.ssl.SOCK_OPT_SSL, min_tls, max_tls)
+    logging.info('==================== NSS INITIALIZATION COMPLETE ====================')
 
 
 def nss_client_auth_callback(unused_ca_names, unused_cert, chosen_nickname):
--- a/src/double_tls.py
+++ b/src/double_tls.py
@@ -573,6 +573,11 @@ class DoubleTLSClient(object):
     def __init__(self, config, hostname, port, cert_nickname):
         self.__config = config
         self.__hostname = hostname
+        
+        logging.info('==================== DOUBLE-TLS CLIENT INIT DEBUG ====================')
+        logging.info('Bridge hostname: %s', hostname)
+        logging.info('Bridge port: %d', port)
+        logging.info('Client cert nickname: %s', cert_nickname)
         self.__port = port
         self.__cert_nickname = cert_nickname
         self.__connection_refused_exit_code = 100
@@ -598,6 +603,7 @@ class DoubleTLSClient(object):
             if first_error is not None:
                 raise first_error
         except Exception:
+            logging.error('DoubleTLSClient.__init__ failed during socketpair/fork', exc_info=True)
             self.__inner_pipe.close()
             self.__outer_pipe.close()
             raise
@@ -769,18 +775,29 @@ class DoubleTLSClient(object):
             outer_pipe_fd = nss.io.Socket.import_tcp_socket(child_outer_pipe.
                                                             fileno())
             utils.nss_init(self.__config)  # May raise utils.NSSInitError
+            
+            logging.info('==================== CHILD PROCESS SSL CONNECTION DEBUG ====================')
+            logging.info('Attempting SSL connection to %s:%d', self.__hostname, self.__port)
+            
             socket_fd = nss.ssl.SSLSocket(nss.io.PR_AF_INET)
             socket_fd.set_ssl_option(nss.ssl.SSL_REQUEST_CERTIFICATE, True)
             socket_fd.set_ssl_option(nss.ssl.SSL_REQUIRE_CERTIFICATE, True)
+            
+            logging.info('Looking up certificate nickname: %s', self.__cert_nickname)
             try:
                 cert = nss.nss.find_cert_from_nickname(self.__cert_nickname)
+                logging.info('✓ Found certificate: %s', cert.subject)
             except nss.error.NSPRError as e:
                 if e.errno == nss.error.SEC_ERROR_BAD_DATABASE:
+                    logging.error('✗ Certificate not found: SEC_ERROR_BAD_DATABASE')
                     raise utils.NSSInitError(
                         'Certificate \'%s\' is not '
                         'available' %
                         self.__cert_nickname)
+                logging.error('✗ Error finding certificate: errno=%d, %s', e.errno, str(e))
                 raise
+            
+            logging.info('Setting client auth callback with certificate')
             socket_fd.set_client_auth_data_callback(
                 utils.nss_client_auth_callback_single, cert)
             socket_fd.set_hostname(self.__hostname)
@@ -788,18 +805,32 @@ class DoubleTLSClient(object):
                                         nss.io.PR_AI_ADDRCONFIG)
             first_error = None
             for net_addr in addr_info:
+                logging.info('Trying address: %s', net_addr.get_ip())
                 net_addr.port = self.__port
                 try:
+                    logging.info('Attempting TCP connect to %s:%d...', self.__hostname, self.__port)
                     socket_fd.connect(net_addr)
+                    logging.info('✓ TCP connection established')
+                    break
                 except Exception as e:
+                    logging.warning('✗ Connection attempt failed: %s', str(e))
                     if first_error is None:
                         first_error = e
             if first_error is not None:
                 if (isinstance(first_error, nss.error.NSPRError)
                         and (first_error.errno
                              == nss.error.PR_CONNECT_RESET_ERROR)):
+                    logging.error('✗ Connection refused by server')
                     raise ChildConnectionRefusedError()
+                logging.error('✗ Connection failed with error: %s', str(first_error))
                 raise first_error
+            
+            logging.info('Starting SSL handshake with bridge...')
             socket_fd.force_handshake()
+            logging.info('✓ SSL handshake with bridge completed successfully')
+            
+            peer_cert = socket_fd.get_peer_certificate()
+            if peer_cert:
+                logging.info('✓ Bridge certificate: %s', peer_cert.subject)
 
             inner_pipe_fd.set_socket_option(
                 nss.io.PR_SockOpt_Nonblocking, True)
@@ -807,6 +838,7 @@ class DoubleTLSClient(object):
                 nss.io.PR_SockOpt_Nonblocking, True)
             socket_fd.set_socket_option(nss.io.PR_SockOpt_Nonblocking, True)
             buf_1 = _CombiningBuffer(inner_pipe_fd, outer_pipe_fd, socket_fd)
+            logging.info('Starting bidirectional forwarding (double-TLS active)...')
             buf_2 = _SplittingBuffer(socket_fd, inner_pipe_fd, outer_pipe_fd)
             _ForwardingBuffer.forward_two_way(buf_1, buf_2)
             inner_pipe_fd.close()
@@ -817,20 +849,28 @@ class DoubleTLSClient(object):
             os._exit(0)
         except ChildConnectionRefusedError:
             logging.debug('Connection refused')
+            logging.error('==================== CHILD EXITING: CONNECTION REFUSED ====================')
             logging.shutdown()
             os._exit(self.__connection_refused_exit_code)
         except nss.error.NSPRError as e:
             if e.errno == nss.error.PR_CONNECT_RESET_ERROR:
                 logging.debug('NSPR error: Connection reset')
+                logging.error('✗ NSPR error: PR_CONNECT_RESET_ERROR (connection reset by peer)')
             elif e.errno == nss.error.SSL_ERROR_EXPIRED_CERT_ALERT:
                 logging.error('Our certificate has been rejected as expired')
                 logging.shutdown()
                 os._exit(self.__unrecoverable_error_exit_code)
             else:
+                logging.error('✗ NSPR error in child: errno=%d', e.errno)
                 logging.warning('Exception in child', exc_info=True)
+            logging.error('==================== CHILD EXITING: NSPR ERROR %d ====================', e.errno)
             logging.shutdown()
             os._exit(1)  # Nothing that extraordinary
         except utils.NSSInitError as e:
             logging.error(str(e))
+            logging.error('==================== CHILD EXITING: NSS INIT ERROR ====================')
+            logging.error('NSS initialization failed: %s', str(e))
+            logging.error('This usually means certificate not found or NSS database issues')
+            logging.error('Check: 1) NSS database path, 2) Certificate nickname, 3) NSS password')
             logging.shutdown()
             os._exit(self.__unrecoverable_error_exit_code)
         except (KeyboardInterrupt, SystemExit):
@@ -838,6 +878,7 @@ class DoubleTLSClient(object):
             logging.shutdown()
             os._exit(self.__unrecoverable_error_exit_code)
         except Exception:
+            logging.error('==================== CHILD EXITING: UNEXPECTED EXCEPTION ====================')
             logging.warning('Exception in child', exc_info=True)
             logging.shutdown()
             os._exit(self.__unrecoverable_error_exit_code)
--- a/src/client.py
+++ b/src/client.py
@@ -103,12 +103,19 @@ class ClientsConnection(object):
 
     def connect(self, op, outer_fields):
         '''Connect and send outer_fields if op, outer_fields is not None.'''
+        logging.info('==================== CLIENT CONNECTION STARTING ====================')
+        logging.info('Operation: %s', op if op else 'None')
+        logging.info('Bridge: %s:%d', self.config.bridge_hostname, self.config.bridge_port)
+        logging.info('Client cert: %s', self.config.client_cert_nickname)
+        
         self.__client = double_tls.DoubleTLSClient(self.config,
                                                    self.config.bridge_hostname,
                                                    self.config.bridge_port,
                                                    self.config.
                                                    client_cert_nickname)
         try:
+            logging.info('Initializing NSS for client connection...')
             utils.nss_init(self.config)
+            logging.info('✓ NSS initialized successfully for client')
         except utils.NSSInitError as e:
             raise ClientError(str(e))
         self.__request_header_writer = \
@@ -1960,8 +1967,12 @@ def main():
                 else:
                     raise Exception('Unhandled child_exception type')
             else:
+                logging.error('==================== CONNECTION ERROR: RESET ====================')
                 logging.error('I/O error: NSPR connection reset')
         elif e.errno == nss.error.PR_END_OF_FILE_ERROR:
+            logging.error('==================== CONNECTION ERROR: UNEXPECTED EOF ====================')
+            logging.error('This typically means the SSL handshake failed or was rejected')
+            logging.error('Check: 1) Bridge is running, 2) Certificates are trusted, 3) NSS database is correct')
             logging.error('I/O error: Unexpected EOF in NSPR')
         else:
             logging.error('NSPR error', exc_info=True)